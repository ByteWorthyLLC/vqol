name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: byteworthyllc/.github/.github/workflows/reusable-ci.yml@main
    with:
      stack: node
